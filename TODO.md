# TODO

Reihenfolge ist bewusst so gewählt — jeder Punkt baut auf dem vorherigen auf. Siehe `PROJECT_STATUS.md` für den Gesamtüberblick und die zwei bisher gefundenen Bugs.

## Phase 1 (Projekt-Setup) — ✅ vollständig abgeschlossen

MCP-Tools verifiziert, `.env` angelegt (URL/Anon-Key via MCP, `service_role`-Key vom Nutzer), Migration `0001_init.sql` live angewendet, alle 1.324 Übungen importiert (`npm run db:seed`). Einziger offener Punkt: **der echte On-Device-Test** (`npm run ios`/`npm run android`, Login/Registrierung, Navigation) — konnte in keiner Background-Job-Session durchgeführt werden (kein Simulator/Emulator verfügbar), muss der Nutzer selbst auf seiner Maschine machen.

## Design System — ✅ abgeschlossen

Auf Nutzerwunsch vor Phase-2-Punkt 2 eingeschoben: vollständiges Design System (Tokens, `Typography`/`Button`/`Card`/`Input`/`ListItem`/`EmptyState`, alle Screens umgestellt). Details + Begründungen in `ARCHITECTURE.md`, Abschnitt „Design System". Recherchiert über den `ui-ux-pro-max`-Skill; `andrej-karpathy-skills` gilt seitdem für den Rest der Arbeit an diesem Projekt.

## Phase 2 der Roadmap (Workout Tracking)

Siehe `ROADMAP.md` Phase 2 und `MVP.md` für die Feature-Definition, `MVP_REVIEW.md` für bekannte Konzept-Lücken.

1. ✅ **Home-Tab**: echte Workout-Historie aus `workouts` (`src/features/home/`). Karten mit Name/Datum/Dauer/Volumen/Anzahl Übungen, `EmptyState` nur wenn wirklich leer, Tap → `/workout/[id]`. **Nachträglich erweitert** um eine Übungsliste mit runden Bildern pro Karte — siehe Abschnitt „Nachgezogene Lücke" unten.
2. ✅ **Übungsauswahl** (`exercise/picker.tsx`, `src/features/exercises/`): debounced Suche, Kategorie-/Geräte-Filter-Chips, Mehrfachauswahl mit Bild-Thumbnails, Attributions-Footer. Wird per `?target=`-Param sowohl von der Routinen- als auch der Workout-Erstellung genutzt.
3. ✅ **Routine erstellen/bearbeiten** (`routine/create.tsx`, `routine/[id]/edit.tsx`, `src/features/routines/`): Name, Übungsauswahl, Reihenfolge, Soll-Sätze/-Wiederholungen/-Gewicht/-Pause. **Architektur-Entscheidung**: ein Zustand-Draft-Store (`routine-draft.store.ts`) statt Navigation-Params, damit die Mehrfachauswahl aus dem Picker zurück ins (weiterhin gemountete) Formular findet, unabhängig von React-Navigation-Mount-Verhalten.
   - *Ungeplante Ergänzung danach* (Nutzer bemerkte: erstellte Routinen waren nirgends sichtbar): **Routinen-Liste** im Training-Tab (`getRoutines()`, `RoutineCard`).
   - *Ungeplante Ergänzung direkt danach* (Nutzer bemerkte: Tap auf Routine startete kein Workout): **"Routine starten"** — befüllt `active-workout.store.ts` mit `target_sets` leeren Sätzen pro Übung und setzt `workouts.routine_id`. Nutzer stellte dafür Hevy-Referenz-Screenshots bereit (`screenshots/`, gitignored), an denen sich die neuen Routinen-Karten (Name + Übungs-Vorschau + Button) orientieren.
4. ✅ **Aktives Workout** (`workout/active.tsx`, `src/features/training/`): Timer, Gesamtvolumen, Satz-Erfassung pro Übung, lokale Zwischenspeicherung. `active-workout.store.ts` nutzt Zustands `persist`-Middleware direkt mit AsyncStorage. `start()`/`startFromRoutine()` sind bewusst idempotent (kein Reset bei erneutem Screen-Aufruf) — das Überleben eines App-Kills ist der Sinn dieses Stores, anders als beim Routine-Draft.
5. ✅ **Workout beenden**: `finishActiveWorkout()` synct `workouts`→`workout_exercises`→`sets` zu Supabase und ruft die `finish_workout`-RPC auf (berechnet `total_volume`/`duration_seconds`, aktualisiert `personal_records`). **Dabei Bug 2 aus `PROJECT_STATUS.md` gefunden+gefixt.**
6. ✅ **Workout-Detail** (`workout/[id].tsx`): zeigt Name, Datum, Dauer, Gesamtvolumen, Übungszahl im Header sowie alle Übungen mit ihren Sätzen (Gewicht/Wiederholungen/completed). `getWorkoutDetail()` in `src/features/home/api/workouts.api.ts` folgt demselben Muster wie `getRoutineForEdit`/`getWorkoutHistory` (flache Queries, in JS gejoint). Gegen die Live-DB verifiziert (siehe `PROJECT_STATUS.md`).
7. ✅ **Übungsdetail** (`exercise/[id].tsx`, `src/features/exercises/`): drei Tabs (Zusammenfassung/Historie/So geht's), orientiert an den Hevy-Referenz-Screenshots. **Bewusste Abweichungen vom Screenshot**: kein interaktives PR-Chart (keine Chart-Lib im Projekt) — stattdessen eine einfache Karte mit dem aktuellen `max_weight`-Rekord + Datum aus `personal_records` (`record_type` ist in der Praxis ohnehin nur `max_weight`, `finish_workout` schreibt nie `max_volume`/`max_reps`/`estimated_1rm`). "So geht's" zeigt `instructions` als Fließtext statt nummerierter Schritte, weil das Feld pro Sprache ein einzelner Absatz ist (kein Schritte-Array); Sprache fällt auf `en` zurück, da der Datensatz kein `de` enthält. **UI-Einstiegspunkt**: Tap auf eine Übung in der Workout-Detailansicht (`workout/[id].tsx`) — die zweite der beiden in Punkt 7 vorgeschlagenen Optionen, da ein zweiter Tap-Handler im Picker mit dessen Mehrfachauswahl kollidiert hätte. Gegen die Live-DB verifiziert (siehe `PROJECT_STATUS.md`).
8. ✅ **Profil-Tab** (`app/(tabs)/profile/index.tsx`, `src/features/profile/`): echte Aggregate (Anzahl Workouts, Trainingszeit, Gesamtvolumen) statt Platzhalter, per `getProfileStats()` aus `workouts` aggregiert (flache Query, in JS summiert, gleiches Muster wie überall sonst). **Bewusste Abweichung von `screenshots/Profile statistics.jpg`**: kein Balkendiagramm und keine Follower/Folgend-Zahlen — beides gehört laut `ROADMAP.md` in spätere Phasen (Diagramme → Phase 3, Social/Follower → Phase 5), nicht in den MVP-Scope von Punkt 8. Nebenbei die bisher direkt im Screen inline liegende `profiles`-Query in ein reguläres `getProfile()`-API-Modul verschoben (Konsistenz mit dem Rest der Codebase, die nie direkt `supabase-js` aus der UI aufruft). Gegen die Live-DB verifiziert (siehe `PROJECT_STATUS.md`).

## Phase 3 der Roadmap (Diagramme & Rekorde)

Siehe `ROADMAP.md` Phase 3. Anders als Phase 2 gibt es dafür noch kein MVP-Dokument mit vorab abgestimmten Einzelpunkten — die folgende Liste ist eine erste Planungsgrundlage (Stand 2026-07-23, noch nicht mit dem Nutzer im Detail abgestimmt), kein fertiges Lastenheft. Noch nichts davon ist umgesetzt.

**Entschieden (Nutzer, 2026-07-23): Option (a).** Charts werden als eigene, minimale Komponenten direkt auf dem bereits vorhandenen `react-native-svg` gebaut (Peer von `lucide-react-native`, siehe `ARCHITECTURE.md`) — kein neuer Dependency (`victory-native`/`react-native-gifted-charts` etc. bewusst nicht installiert). Konkret heißt das für Punkt 1: eine eigene `<Svg>`-Komponente (vermutlich `src/shared/components/LineChart.tsx` oder als übungs-spezifische Komponente in `src/features/exercises/components/`), die aus den Datenpunkten `Path`/`Line`/`Circle`-Elemente selbst berechnet — keine externe Chart-API, an der sich orientiert werden könnte.

1. ✅ **PR-Diagramm im Übungsdetail** (`exercise/[id].tsx`, Tab „Zusammenfassung"): eigene `LineChart`-Komponente (`src/shared/components/LineChart.tsx`, `react-native-svg` `Path`/`Circle` auf einem festen `viewBox`, `preserveAspectRatio="none"` für responsive Breite bei fixer Höhe). **Keine neue Query nötig** — die Historie kommt aus dem bereits vorhandenen `getExerciseHistory()` (Phase 2, Punkt 7); dessen `enabled`-Flag war bisher an den „Historie"-Tab gekoppelt, ist jetzt `!!session` (immer laden, sobald eingeloggt), da der Chart auf „Zusammenfassung" dieselben Daten braucht. Ein Datenpunkt pro Workout = das schwerste completed Gewicht dieser Übung in diesem Workout (spiegelt `finish_workout`s eigene `max_weight`-Definition), chronologisch aufsteigend (die Historie-Query liefert neueste zuerst, für den Chart umgedreht). Gegen die Live-DB verifiziert: drei Test-Workouts mit unterschiedlichen Gewichten an unterschiedlichen Tagen angelegt, Reihenfolge/Aggregation nach dem `reverse()`/`map()` bestätigt korrekt (nicht nach Wert sortiert, sondern nach Datum), Test-Daten danach gelöscht.
2. ✅ **One Rep Max** (`supabase/migrations/0003_add_estimated_1rm_personal_record.sql`, live angewendet): `finish_workout` schreibt jetzt zusätzlich zu `max_weight` auch `record_type = 'estimated_1rm'` (Epley-Formel `weight * (1 + reps/30)`, gerundet auf 2 Nachkommastellen), unabhängig per eigenem `DISTINCT ON (exercise_id)`-Block berechnet — ein leichteres Satz mit mehr Wiederholungen kann hier gewinnen, obwohl es beim `max_weight` verliert (bewusst kein gemeinsamer Sieger-Satz). `getExercisePersonalRecord()` in `exercises.api.ts` wurde zu `getExercisePersonalRecords()` (Plural) verallgemeinert — eine Query für beide `record_type`-Werte statt zweier separater Aufrufe, gibt `{ maxWeight, estimated1Rm }` zurück. Übungsdetail (Tab „Zusammenfassung") zeigt beide Karten nebeneinander. Gegen die Live-DB verifiziert: Testfall mit einem schweren Satz mit wenig Wiederholungen und einem leichteren Satz mit mehr Wiederholungen bestätigte, dass `max_weight` und `estimated_1rm` tatsächlich von unterschiedlichen Sätzen stammen können (100 kg × 1 gewinnt `max_weight`, 90 kg × 5 gewinnt `estimated_1rm` mit 105.00); zweiter Testfall bestätigte den reinen Formel-Wert (60 kg × 6 → 72.00). Test-Daten danach jeweils gelöscht.
3. ✅ **Muskel-Split** (`app/(tabs)/profile/index.tsx`, `src/features/profile/`): Gesamtvolumen pro Muskelgruppe (`exercises.target_muscle`, all-time, keine Zeitraum-Auswahl — kein Screen im Projekt hat bisher einen Datumsfilter), als horizontale Balken in einer neuen `Card` unterhalb der bestehenden `ProfileStatsCard`. **Landeplatz-Entscheidung**: Profil-Tab statt eigenem Screen — kein neuer Navigations-Einstiegspunkt nötig, passt inhaltlich neben die dortigen Aggregate. `getMuscleSplit()` folgt dem etablierten Flat-Query-Muster (`workouts` → `workout_exercises` → `sets` → `exercises.target_muscle`, in JS mit einer `Map` pro Muskelgruppe aufsummiert, absteigend sortiert). Neue Komponente `src/shared/components/BarChart.tsx` (`react-native-svg` `Rect` auf zwei übereinanderliegenden Balken pro Zeile — Track + Fortschritt —, gleicher `viewBox`/`preserveAspectRatio="none"`-Ansatz wie `LineChart`). Gegen die Live-DB verifiziert: zwei verschiedene Übungen mit demselben `target_muscle` in einem Test-Workout bestätigten, dass die Aggregation korrekt über Übungen hinweg summiert (nicht nur pro Übung), Test-Daten danach gelöscht.
4. ✅ **Rekorde aktiv nutzen** (`app/records.tsx`, neues Feature `src/features/records/`): eigene Übersicht aller persönlichen Rekorde — eine Liste aller Übungen mit `personal_records`-Zeilen, je Zeile „Bestes Gewicht" (`max_weight`) und „Geschätztes 1RM" (`estimated_1rm`), alphabetisch nach Übungsname sortiert, Tap → `exercise/[id]` (schließt den Kreis zurück zur Einzelansicht aus Punkt 7). `getPersonalRecords()` folgt dem etablierten Flat-Query-Muster: eine Query über `personal_records` (beide `record_type`-Werte), eine Query über `exercises` für die Namen, in JS pro `exercise_id` zu einer Zeile zusammengeführt. **Neuer Top-Level-Screen** (`app/records.tsx`, in `app/_layout.tsx`s `Stack.Protected`-Gruppe registriert) statt einer Erweiterung eines bestehenden Screens, da es sich um eine eigenständige, übungsübergreifende Ansicht handelt, die weder ins Übungsdetail (pro Übung) noch in den Profil-Tab (Aggregate/Muskel-Split) passt. Einstiegspunkt: neuer „Rekorde ansehen"-Button im Profil-Tab. Gegen die Live-DB verifiziert: zwei Übungen mit je einem Satz in einem Test-Workout bestätigten sowohl die korrekte Zusammenführung von `max_weight`+`estimated_1rm` pro Übung als auch die alphabetische Sortierung, Test-Daten danach gelöscht.

## Nachgezogene Lücke: Übungs-Thumbnails (vor Phase 4, ungeplant)

Der Nutzer bemerkte beim Übergang zu Phase 4, dass im Home-Bereich (anders als in `screenshots/Home.jpg`) keine runden Übungsbilder auftauchen. **Untersuchung des Befunds:** `MVP.md`s ursprüngliche Home-Spezifikation listet für jede Karte nur 5 Felder (Name/Datum/Dauer/Volumen/Anzahl Übungen) — keine Übungsliste mit Bildern. `MVP_REVIEW.md` markiert Diagramme/Rekorde/Muskelverteilung explizit als "bewusst nicht als Schwachstelle gewertet... erst Phase 3", aber es gibt **keine vergleichbare Notiz zu Übungs-Thumbnails** in `MVP_REVIEW.md` oder `ROADMAP.md`. Ergebnis: **echte, bisher undokumentierte Lücke** (nicht bewusst auf eine spätere Phase verschoben) — beim Übersetzen der Hevy-Screenshots in `MVP.md` wurde dieses Detail schlicht nicht mit übernommen.

Auf Nutzerwunsch vor Phase 4 nachgezogen:

- **Home-Karten** (`WorkoutHistoryCard.tsx`): zeigen jetzt bis zu 3 Übungen mit rundem Bild + „N Sätze <Name>", darunter „Sehe N weitere Übungen" falls mehr vorhanden. `getWorkoutHistory()` dafür von einem `workout_exercises(count)`-Embed auf vier flache Queries umgestellt (`workouts` → `workout_exercises` → `sets`/`exercises`, in JS gejoint) — liefert jetzt Name/Bild/Satzanzahl pro Übung statt nur eine Gesamtzahl.
- **Workout-Detail** (`WorkoutDetailExerciseCard.tsx`) und **Übungsdetail-Historie** (`ExerciseHistoryEntryCard.tsx`): beide zeigen jetzt ebenfalls das runde Übungsbild (Workout-Detail: aus der bereits vorhandenen `getWorkoutDetail()`-Query, jetzt mit `image_url`; Übungsdetail-Historie: das Bild der aktuell betrachteten Übung wird vom übergeordneten Screen durchgereicht, keine neue Query nötig, da im Historie-Tab ohnehin immer dieselbe Übung gemeint ist).
- **Formkonsistenz-Fix**: `exercise/picker.tsx`s Thumbnails waren `rounded-md` (eckig) statt `rounded-full` (rund) wie überall in den Hevy-Referenzen — angeglichen.

Gegen die Live-DB verifiziert: ein Test-Workout mit 4 Übungen (unterschiedliche Satzanzahl je Übung, `order_index`-Reihenfolge) bestätigte, dass `getWorkoutHistory()` Name/Bild/Satzanzahl korrekt pro Übung liefert und die Karten-Logik (erste 3 sichtbar + „1 weitere") die richtige Übung abschneidet, Test-Daten danach gelöscht.

## Phase 4 der Roadmap (Komfort-Features)

Siehe `ROADMAP.md` Phase 4. Wie bei Phase 3 gibt es dafür kein MVP-Dokument mit vorab abgestimmten Einzelpunkten — die folgende Liste ist eine erste technische Bestandsaufnahme (Stand 2026-07-24, noch nicht mit dem Nutzer priorisiert), kein fertiges Lastenheft. Anders als bei Phase 3 sind die sieben Themen hier deutlich heterogener (Datenmodell-Arbeit, neue Dependency, reine Client-Logik, echte Produktentscheidungen) — deshalb vor dem ersten Punkt eine Reihenfolge/Klärung mit dem Nutzer statt einfach der Roadmap-Reihenfolge zu folgen.

1. ✅ **Notizen**: alle drei bereits im Schema vorhandenen `notes`-Spalten (`routines`, `workouts`, `workout_exercises`) sind jetzt an UI angebunden, keine neue Migration nötig. **Routine-Notizen**: neues Textfeld in `RoutineForm.tsx`, im `routine-draft.store.ts` neben `name` gehalten, über `createRoutine()`/`updateRoutine()`/`getRoutineForEdit()` (alle um einen `notes`-Parameter erweitert) persistiert. **Workout-Notizen** (workout-weit): neues Textfeld in `workout/active.tsx`, im `active-workout.store.ts` gehalten, über `finishActiveWorkout()` in `workouts.notes` geschrieben, in `workout/[id].tsx` unter dem Header angezeigt. **Pro-Übung-Notizen** (`workout_exercises.notes`): neues Textfeld in `ActiveWorkoutExerciseCard.tsx` (`onUpdateNotes`-Prop, `updateExerciseNotes()`-Action im Store), über `finishActiveWorkout()` in `workout_exercises.notes` geschrieben, in `WorkoutDetailExerciseCard.tsx` angezeigt. Gegen die Live-DB verifiziert: alle drei Notiz-Ebenen einzeln geschrieben und mit denselben Select-Formen wie `getRoutineForEdit()`/`getWorkoutDetail()` zurückgelesen, Werte kamen unverändert zurück, Test-Daten danach gelöscht.
2. ✅ **Custom Exercises**: neue `createCustomExercise(userId, input)` in `exercises.api.ts` — inserted `is_custom: true, created_by: userId` zusammen (von der DB-Check-Constraint `custom_exercise_requires_owner` und der RLS-Policy "custom exercises are owner-writable" ohnehin gemeinsam erzwungen), gibt ein `ExerciseListItem` zurück (gleiche Form wie `getExercises`). Neue Komponente `src/features/exercises/components/CustomExerciseForm.tsx` (Name-Input, Kategorie-/Geräte-Auswahl als `FilterChip`-Reihen aus den bereits vorhandenen `EXERCISE_CATEGORIES`/`EXERCISE_EQUIPMENT`-Konstanten, Zielmuskel- und optionales Bild-URL-Freitextfeld, `useState` + manuelle Validierung nach `RoutineForm.tsx`-Muster). Neuer Screen `app/exercise/create.tsx` (in `app/_layout.tsx`s `Stack.Protected`-Gruppe als Modal registriert), reicht `target`/`search`-Params vom Picker durch, fügt die neu erstellte Übung nach dem Speichern direkt in den passenden Draft-Store (`routine-draft.store.ts` bzw. `active-workout.store.ts`, je nach `target`) ein und invalidiert die `exercises`-Query, damit die neue Übung bei erneutem Öffnen des Pickers mit auftaucht. **Einstiegspunkt**: neuer „+ Eigene Übung erstellen"-Button (`Button variant="ghost" size="sm"`) im Picker (`app/exercise/picker.tsx`), direkt unter den Filter-Chips — bewusst immer sichtbar statt nur im Leerzustand, damit eine eigene Übung auch dann erstellt werden kann, wenn die Suche bereits Treffer liefert. **Bewusste Vereinfachung**: kein Bild-Upload (kein `expo-image-picker`, kein Supabase-Storage-Bucket im Projekt vorhanden, siehe Recherche) — stattdessen ein einfaches optionales Bild-URL-Textfeld, konsistent damit, dass `image_url` in der bestehenden App ohnehin nur eine externe URL-Spalte ist (alle bisherigen Bilder kommen vom Gym-visual-Datensatz, kein eigenes Upload-Pattern existiert). Kategorie/Gerät werden aus der bestehenden kuratierten Liste ausgewählt (kein Freitext) — die bestehende Code-Notiz zu `EXERCISE_EQUIPMENT` (evtl. erweiterungsbedürftig für neue Geräte) bleibt unverändert gültig, aber außerhalb des Scopes dieses Punkts. Gegen die Live-DB verifiziert: Insert exakt in der Form, die `createCustomExercise` erzeugt (`is_custom: true`, `created_by: <echte User-ID>`), bestätigte die RLS-Policy-Bedingung (`with_check: is_custom = true AND created_by = auth.uid()`), die Zeile erschien korrekt in derselben Select-Form wie `getExercises`, Test-Zeile danach gelöscht.
3. ✅ **Rest-Timer**: `routine_exercises.rest_seconds` wurde bereits von `getRoutineForEdit()` geladen und bis zum Store durchgereicht, aber laut `active-workout.store.ts`s vorherigem Kommentar bewusst nie in den aktiven Workout-State übernommen. Jetzt: `RoutineExerciseSeed` (Store-interner Typ) um `restSeconds: number | null` erweitert, `ActiveWorkoutExercise` (`active-workout.types.ts`) trägt es jetzt pro Übung mit (`null` für per Picker ad-hoc hinzugefügte Übungen ohne Routine-Zielwert — dort gibt's ohnehin keinen erfassten Sollwert). **Kein reiner "Sekunden verbleibend"-Zähler im Store** (würde nach einem App-Kill veraltet sein), sondern ein absoluter Endzeitpunkt `restEndsAt: string | null` (gleiches Muster wie `startedAt`) — überlebt App-Kills korrekt, weil beim Neuladen einfach `Math.max(0, endMs - Date.now())` neu berechnet wird statt eines gespeicherten Countdown-Werts. Neue Store-Actions `startRestTimer(seconds)`/`clearRestTimer()`, zurückgesetzt in `start()`/`startFromRoutine()`/`reset()`. In `app/workout/active.tsx`: neuer `useRemainingSeconds(endsAt, onComplete)`-Hook (spiegelt den bestehenden `useElapsedSeconds`-Hook, aber als Countdown statt Count-up, ruft `onComplete` einmalig bei Erreichen von 0 auf). `handleToggleSetCompleted()` erkennt gezielt den false→true-Übergang (nicht jeden Tap — sonst würde auch das Abwählen eines Satzes einen Timer starten) und startet den Timer nur dann, mit dem `restSeconds`-Wert der jeweiligen Übung. UI: eine Pause-Anzeige (`mm:ss` + „Überspringen"-Button) zwischen der Volumen/Sätze-Stat-Zeile und dem Namensfeld, nur sichtbar während `restEndsAt` gesetzt ist. **Keine neue Dependency** (kein `expo-haptics`/`expo-notifications` für Sound/Vibration/Hintergrund-Benachrichtigung bei Timer-Ende — bewusst außerhalb des Scopes, rein visueller Countdown reicht für diesen Punkt; im Projekt bisher ohnehin nicht installiert). Keine DB-Änderung nötig (`rest_seconds` existierte bereits, wurde nur nicht bis in den aktiven Workout durchgereicht) — rein clientseitige Änderung, gegen `npx tsc --noEmit`, `npm run lint`, `npx expo export --platform ios` verifiziert.
4. ⬜ **Plattenrechner**: keine DB-Änderung nötig — reine Client-Logik (Zielgewicht + Stangentyp → Plattenkombination pro Seite) + UI. Einstiegspunkt vermutlich aus dem Gewichts-Input im aktiven Workout heraus.
5. ⬜ **Favoriten**: keine bestehende Spalte/Tabelle dafür. Braucht eine neue Migration (vermutlich eine `favorite_exercises`-Tabelle `user_id, exercise_id` statt einer Spalte, für spätere Erweiterbarkeit). **Offene Frage:** nur Übungen, oder auch Routinen favorisierbar?
6. ⬜ **Kalender**: **mehrdeutig, mit dem Nutzer zu klären**, bevor irgendetwas gebaut wird — `ROADMAP.md` sagt nur "Kalender", nicht ob damit (a) eine Kalender-Ansicht der Trainings-Historie (Monatsraster, Tap auf einen Tag → Workout dieses Tages) oder (b) eine Trainingsplanung (Routinen bestimmten Wochentagen/Daten im Voraus zuweisen) gemeint ist. Beide sind plausibel, unterschiedlich groß, und keine bisherige Doku (`MVP.md`, `MVP_REVIEW.md`) grenzt das genauer ein.
7. ⬜ **Workout-Erinnerungen**: größter Einzelpunkt. Braucht `expo-notifications` (neuer Dependency, noch nicht installiert) + Berechtigungs-Flow. Zwei Architektur-Varianten mit sehr unterschiedlichem Aufwand: **(a)** rein lokale, geräteseitig geplante Erinnerungen (kein Server nötig, aber überlebt keine Neuinstallation/kein Multi-Device) oder **(b)** serverseitige Planung (Supabase Cron/Edge Function + gespeicherte Push-Tokens, deutlich mehr Infrastruktur, aber robuster). Kann in dieser Umgebung ohnehin nicht auf einem echten Gerät verifiziert werden (gleiche Einschränkung wie beim ausstehenden On-Device-Test) — Push-Zustellung selbst bliebe bis zum echten Test unverifiziert.

## Nächster Schritt

**Punkte 1 (Notizen), 2 (Custom Exercises) und 3 (Rest-Timer) sind erledigt.** Als Nächstes Punkt 4 (Plattenrechner) — reine Client-Logik, ohne offene Architekturfragen. Favoriten (Punkt 5) braucht vorher eine neue Migration, Kalender (Punkt 6) und Workout-Erinnerungen (Punkt 7) brauchen vorher eine Klärung mit dem Nutzer (siehe oben). Alternativ weiterhin der ausstehende echte On-Device-Test durch den Nutzer selbst (siehe `PROJECT_STATUS.md`, „Was funktioniert") — inklusive des Rest-Timers, der bisher nur gegen `tsc`/`lint`/Bundling verifiziert wurde, nicht auf einem echten Gerät.

## Sonstige offene Punkte

- **Nicht neu entscheiden, nur beachten:** `exercises-dataset-main/` und `screenshots/` sind beide bewusst `.gitignore`d (erstere: einmaliger Seed-Bedarf, 2.661 Dateien; letztere: Referenz-Screenshots einer dritten App, keine eigene IP).
- **Keine Test-Accounts mehr anlegen** (Supabase-Signups) — siehe Memory `no-test-signups`, hat eine Warnmail beim Nutzer ausgelöst. DB-Verifikationen laufen über direkte SQL-Operationen mit der echten, bestehenden Nutzer-ID.
- `src/shared/types/database.types.ts` ist aus dem Live-Schema generiert (`mcp__supabase__generate_typescript_types`), nicht mehr handgeschrieben. Falls striktere Typen (z. B. für `category`, `set_type`) gewünscht sind: als eigene Union-Types **neben** dem generierten `Database`-Typ ergänzen, nicht die generierte Datei von Hand bearbeiten (geht bei der nächsten Regenerierung sonst verloren).
- `routine/[id]/edit.tsx`, `workout/[id].tsx` und `exercise/[id].tsx` sind alle fertig gebaut und über echte Daten erreichbar. Kein Screen ohne Einstiegspunkt mehr.
