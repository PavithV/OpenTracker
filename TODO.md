# TODO

Reihenfolge ist bewusst so gewählt — jeder Punkt baut auf dem vorherigen auf.

## Sofort (neue Session)

1. ~~**MCP-Tools verifizieren**~~ ✅ Erledigt (2026-07-23): In dieser Session waren die `mcp__supabase__*`-Tools direkt über `ToolSearch` auffindbar und funktionsfähig (Vermutung aus der letzten Session bestätigt: Background-Jobs übernehmen die Tool-Liste offenbar doch, sofern die MCP-Verbindung schon vor Session-Start stand — war hier der Fall).
2. **`.env` anlegen** — teilweise erledigt: `EXPO_PUBLIC_SUPABASE_URL` (`https://rlcrhsubxcsjbqpgrwvs.supabase.co`) und `EXPO_PUBLIC_SUPABASE_ANON_KEY` liegen bereits vor (per `mcp__supabase__get_project_url` / `get_publishable_keys` abgerufen). **Fehlt noch**: `SUPABASE_SERVICE_ROLE_KEY` — der wird von den MCP-Tools aus Sicherheitsgründen nicht herausgegeben, muss manuell aus dem Supabase-Dashboard (Settings → API → service_role) geholt und in `.env` eingetragen werden, bevor Punkt 4 laufen kann.
3. ~~**Migration anwenden**~~ ✅ Erledigt (2026-07-23): `supabase/migrations/0001_init.sql` per `mcp__supabase__apply_migration` gegen das Live-Projekt ausgeführt. Alle 8 Tabellen existieren mit aktiviertem RLS (`list_tables` verifiziert), `get_advisors` zeigt nur die erwarteten (unkritischen) Warnungen zu den beiden `SECURITY DEFINER`-Funktionen. `src/shared/types/database.types.ts` wurde daraufhin durch `mcp__supabase__generate_typescript_types` ersetzt (siehe „Nicht vergessen" unten) — `tsc --noEmit` und `npm run lint` bleiben sauber.
4. ~~**Übungen importieren**~~ ✅ Erledigt (2026-07-23): Nutzer hat den `service_role`-Key bereitgestellt, `.env` liegt jetzt sowohl im Hauptcheckout als auch im Session-Worktree. `npm run db:seed` lief durch (1.324/1.324 Übungen, keine Fehler). Storage-Bucket `exercise-media` wurde vom Skript automatisch angelegt (`public: true`). Verifiziert per SQL: `count(*) = 1324`, `count(image_url) = count(gif_url) = 1324` (alle Zeilen haben Medien-URLs).
5. **Smoke-Test** — teilweise erledigt, vom Nutzer selbst auf später verschoben (2026-07-23, wegen Supabase Email-Rate-Limit beim Registrieren). Auf API-Ebene bereits verifiziert (kein Simulator/Emulator in der Background-Job-Session verfügbar): `GET /rest/v1/exercises` mit dem `anon`-Key liefert echte Zeilen, Bild-URLs aus `exercise-media` sind öffentlich per HTTP 200 erreichbar. **Dabei ein echter Bug gefunden und gefixt**: Registrierung schlug fehl, weil Supabase "Confirm email" standardmäßig aktiv ist — `signUp()` gelang zwar (Auth-User + Profil wurden korrekt angelegt), lieferte aber keine Session zurück, und `sign-up.tsx` hat trotzdem bedingungslos zu `(tabs)` weitergeleitet, wurde dann vom Auth-Gate ohne jede Fehlermeldung zurück zu `(auth)` geschickt. Direkt gegen die Live-Auth-API reproduziert und behoben: `signUpWithEmail` gibt jetzt zurück, ob eine Bestätigung aussteht, `sign-up.tsx` zeigt in dem Fall einen Hinweis statt fälschlich "eingeloggt" zu tun. **Noch offen**: der eigentliche On-Device-Test (`npm run ios`/`npm run android`, Login/Registrierung, Navigation) — das muss der Nutzer selbst auf einer Maschine mit Simulator/Emulator/Gerät durchführen, sobald das Email-Rate-Limit abgeklungen ist (oder "Confirm email" im Dashboard vorübergehend deaktivieren, um schneller zu testen).

## Offene Entscheidung

- ~~**`exercises-dataset-main/` in Git tracken oder nicht?**~~ ✅ Entschieden (2026-07-23), Option (c): bleibt untracked, jetzt explizit in `.gitignore` (`/exercises-dataset-main`) statt nur zufällig nicht committet, README dokumentiert den manuellen Download-Schritt vor `npm run db:seed`. Begründung: Der Ordner wird nur einmalig für den Seed-Lauf gebraucht, 2.661 Dateien (viele Bilder) würden das Repo unnötig aufblähen.

## Danach: Phase 2 der Roadmap (Workout Tracking)

Siehe `ROADMAP.md` Phase 2 und `MVP.md` für die Feature-Definition, `MVP_REVIEW.md` für bekannte Lücken, die dabei mit bedacht werden sollten (z. B. Satz-Typen, Routinen-Sollwerte, lokale Zwischenspeicherung des aktiven Workouts). Konkret zu bauen, in dieser Reihenfolge:

**Zwischenstopp (2026-07-23):** Vor Punkt 2 wurde auf Nutzerwunsch erst ein vollständiges Design System gebaut (Tokens, Typography/Button/Card/Input/ListItem/EmptyState, alle Screens darauf umgestellt) — siehe `ARCHITECTURE.md`, Abschnitt „Design System". Recherchiert über den `ui-ux-pro-max`-Skill; `andrej-karpathy-skills` gilt seitdem für den Rest der Session.

1. ~~Home-Tab: echte Workout-Historie aus `workouts` laden~~ ✅ Erledigt (2026-07-23): `src/features/home/{api,components,types}` befüllt (`getWorkoutHistory`, `WorkoutHistoryCard`), `app/(tabs)/home/index.tsx` lädt per TanStack Query nur abgeschlossene Workouts (`ended_at is not null`) des eingeloggten Users, zeigt Karten (Name/Datum/Dauer/Volumen/Anzahl Übungen via eingebettetem `workout_exercises(count)`), `EmptyState` nur wenn Liste wirklich leer, Tap navigiert zu `/workout/[id]`. Ungetestet gegen echte Daten, da noch niemand ein Workout abgeschlossen hat (hängt an `workout/active.tsx`, Punkt 4/5 unten) — `tsc`/`lint`/Bundling-Smoke-Test bleiben aber sauber.
2. ~~Übungsauswahl (`exercise/picker.tsx` ausbauen)~~ ✅ Erledigt (2026-07-23): `src/features/exercises/{api,components,types}` befüllt (`getExercises` mit `search`/`category`/`equipment`-Filtern über `ilike`/`eq`, `FilterChip`). Screen hat jetzt: debounced Suche (300ms), Kategorie- und Geräte-Filter als horizontal scrollbare Chip-Reihen (`EXERCISE_CATEGORIES` aus dem DB-Check-Constraint, `EXERCISE_EQUIPMENT` hartkodiert aus den 28 beobachteten Werten im Seed-Datensatz — `equipment` ist nicht enum-constrained, siehe Kommentar in `exercise.types.ts`), Mehrfachauswahl (lokaler `Set<string>`-State, Checkmark-Icon in `ListItem`s `trailing`-Slot, Anzahl-Anzeige), Bild-Thumbnail in `ListItem`s `leading`-Slot (rohes RN `Image`, kein `expo-image` — bewusst keine neue Dependency ohne Testmöglichkeit diese Session), Attributions-Footer ("© Gym visual — https://gymvisual.com/", vorher in keinem Screen sichtbar). Filter-Kombination gegen die Live-DB verifiziert (`ilike`+`eq`+`eq` liefert plausible Treffer). **Bewusst nicht gebaut**: eine "Fertig/Weiter"-Aktion für die Auswahl — es gibt noch kein Ziel dafür, das ist Punkt 3.
3. ~~Routine erstellen/bearbeiten~~ ✅ Erledigt (2026-07-23): `src/features/routines/{types,store,api,components}` neu. Kernentscheidung: ein Zustand-Draft-Store (`routine-draft.store.ts`, gleiches Muster wie `session.store.ts` / das für `features/training/store` vorgesehene Muster aus `ARCHITECTURE.md`) statt Navigation-Params, damit die Mehrfachauswahl aus `exercise/picker.tsx` zurück zum (weiterhin gemounteten) Formular findet, ohne von React-Navigation-Mount-Verhalten abhängig zu sein. `exercise/picker.tsx` hat jetzt eine "Fertig"-Leiste (eigene `SafeAreaView edges={['bottom']}`, da `Screen` keinen Bottom-Inset reserviert), die die Auswahl in den Draft schreibt und zurücknavigiert. `RoutineForm` (geteilt von create+edit) validiert (Name + ≥1 Übung), speichert, setzt den Draft zurück und geht zurück zum Training-Tab. `RoutineExerciseRow` zeigt Reorder-Pfeile (rohe `Pressable`+lucide-Icons, bewusst kein Icon-Only-`Button`, um dessen bereits abgeschlossene API nicht wieder anzufassen), Entfernen-Button und 5 kompakte Zahlenfelder (Sätze/Wdh. min/Wdh. max/Gewicht/Pause). `updateRoutine` löscht+re-inserted `routine_exercises` komplett statt zu diffen (unkritisch, da nichts anderes auf `routine_exercises`-Zeilen per FK verweist). **Verifiziert**: kompletter Create→Update-Ablauf per SQL gegen die Live-DB durchgespielt (Testroutine mit 2 Übungen angelegt, umbenannt, Übungen ersetzt/Zielwerte geändert, danach alles wieder gelöscht) — echte RLS-Durchsetzung als authentifizierter Nutzer war nicht testbar (kein bestätigter Nutzer mit Passwort-Login verfügbar), aber die Policy-Struktur ist identisch zu der bereits geprüften `workouts`/`workout_exercises`-Struktur. **Noch offen**: es gibt weiterhin keine Routinen-Listen-Ansicht, daher bleibt `routine/[id]/edit.tsx` erreichbar-aber-ohne-Einstiegspunkt (wie `workout/[id].tsx` vor dem Home-Tab-Ausbau).
4. ~~Aktives Workout~~ ✅ Erledigt (2026-07-23): `src/features/training/{types,store,components}` neu. `active-workout.store.ts` nutzt Zustands `persist`-Middleware direkt mit AsyncStorage (`createJSONStorage(() => AsyncStorage)`) — kein Handrollen von Save/Load-Logik, kein neues Package (Zustand + AsyncStorage waren schon Dependencies). `start()` ist bewusst idempotent (No-Op, wenn schon ein Workout läuft) statt bedingungslos zurückzusetzen wie beim Routine-Draft — hier ist das Überleben eines App-Kills der ganze Sinn des Features, ein Reset bei jedem erneuten Öffnen von `workout/active.tsx` würde genau das kaputt machen. `workout/active.tsx` ruft `start()` idempotent selbst in einem `useEffect` auf (Screen besitzt seine eigene Initialisierung, kein Verlass darauf, dass der Training-Tab-Button daran denkt). Timer läuft rein lokal (`setInterval`, nicht persistiert — nur `startedAt` wird gespeichert, daraus wird die verstrichene Zeit abgeleitet), Gesamtvolumen/Satz-Anzahl werden aus den Sätzen abgeleitet (`computeTotalVolume`/`computeCompletedSetCount`, spiegeln exakt `finish_workout`s SQL-Berechnung). `exercise/picker.tsx` unterscheidet jetzt per `?target=workout`-Param, ob die Auswahl in den Routine- oder den Workout-Draft geschrieben wird. **Bewusst nicht gebaut**: der "Workout beenden"-Button ist sichtbar aber `disabled` (kein Fake-Button, der etwas vortäuscht) — Sync zu Supabase + `finish_workout`-RPC ist Punkt 5. Kein Discard/Abbrechen-Aktion gebaut (MVP.md erwähnt keine); das X oben schließt nur die Modal-Ansicht, der Draft bleibt lokal erhalten. Keine DB-Schreibvorgänge nötig für diesen Punkt (rein lokal per `ARCHITECTURE.md`), daher auch kein Testaccount nötig — der Nutzer hat gebeten, keine mehr anzulegen (siehe Memory `no-test-signups`), Verifikation lief über `tsc`/Lint/Bundling-Export.
5. Workout beenden → `finish_workout`-RPC aufrufen (bereits in der Migration implementiert)
6. Workout-Detail (`workout/[id].tsx`): alle Übungen/Sätze anzeigen
7. Übungsdetail (`exercise/[id].tsx`): Tabs Zusammenfassung/Historie, persönlicher Rekord aus `personal_records`
8. Profil-Tab: echte Aggregate (Anzahl Workouts, Trainingsminuten, Gesamtvolumen) statt Platzhalter

## Nicht vergessen

- `exercise/[id].tsx` ist der letzte verbliebene Platzhalter-Screen ohne UI-Einstiegspunkt — bei Punkt 7 die Navigation dorthin ergänzen (z. B. Tap auf eine Übung im Picker oder in der Workout-Detailansicht). `workout/[id].tsx` hat seit dem Home-Tab-Ausbau einen echten Einstiegspunkt (Tap auf eine Verlaufskarte), bleibt aber praktisch unerreichbar, bis `finish_workout` (Punkt 5) tatsächlich Workouts abschließt. `workout/active.tsx` ist über den "Leeres Workout starten"-Button im Training-Tab erreichbar und seit Punkt 4 voll funktionsfähig (bis auf den Save-Schritt, Punkt 5). `routine/create.tsx` ist über den "Routine erstellen"-Button im Training-Tab erreichbar und voll funktionsfähig; `routine/[id]/edit.tsx` ist fertig gebaut, aber weiterhin ohne Einstiegspunkt, da es noch keine Routinen-Listen-Ansicht gibt (nicht Teil von Punkt 3 — kommt vermutlich mit „Routine starten").
- ~~`src/shared/types/database.types.ts` ist handgeschrieben...~~ ✅ Erledigt (2026-07-23): durch `mcp__supabase__generate_typescript_types` gegen das jetzt live geschaltete Schema ersetzt. Die bisherigen String-Literal-Unions (`UnitPreference`, `SetType`, `PersonalRecordType`, `Instructions`) sind dabei entfallen, da der generierte Typ diese Spalten als `string`/`Json` führt und nirgends im Code (nur `supabase.ts` importiert `Database`) auf die engeren Literale angewiesen war — geprüft per Grep vor dem Ersetzen. Falls in Phase 2 striktere Typen für `category`, `unit_preference`, `set_type` etc. gewünscht sind, separat als eigene Union-Types neben dem generierten `Database`-Typ ergänzen statt die generierte Datei erneut von Hand zu bearbeiten (sonst geht das bei der nächsten Regenerierung wieder verloren).
